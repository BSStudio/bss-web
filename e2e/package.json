{
  "name": "e2e",
  "version": "1.0.0",
  "scripts": {
    "pretest": "docker-compose up --wait --build",
    "test": "playwright test",
    "posttest": "docker-compose down"
  },
  "private": true,
  "dependencies": {
    "@playwright/test": "^1.17.1"
  }
}
